### 테스트 시나리오 가이드만 유지 (엔드포인트 제거)
# 별도의 /terms/check 컨트롤러가 존재하지 않아 요청 섹션을 제거했습니다.

### 참고: 인증 시나리오
# 1) auth.http 의 "이메일 로그인 링크 전송(by-user)" 실행 → magicLink 추출
# 2) "이메일 매직 링크 콜백 (백엔드 검증)" 실행 → 302 응답 Location 해시의 token 값을 세션 토큰으로 사용
# 3) 그 토큰을 Bearer 로 Authorization 헤더에 설정하여 다른 API 호출
