
### 사용자 미션 활동 상세 조회
GET {{host}}/api/user-activities/1
Accept: application/json

> {%
client.test("사용자 미션 활동 상세 조회 - 200 또는 404", () => {
  client.assert([200,404].includes(response.status), "Expected 200 or 404");
});
%}

### 퀴즈 답변 제출 (텍스트)
POST {{host}}/api/user-activities/missions/1/submit-quiz?userId={{userId}}&textAnswer=정답
Accept: application/json

> {%
client.test("퀴즈 답변 제출(텍스트) - 200", () => {
  client.assert(response.status === 200, "Status must be 200 OK");
});
%}

### 퀴즈 답변 제출 (선택지)
POST {{host}}/api/user-activities/missions/1/submit-quiz?userId={{userId}}&selectedChoiceIndex=0
Accept: application/json

> {%
client.test("퀴즈 답변 제출(선택지) - 200", () => {
  client.assert(response.status === 200, "Status must be 200 OK");
});
%}

### 퀴즈 답변 제출 (이미지)
POST {{host}}/api/user-activities/missions/1/submit-quiz?userId={{userId}}&uploadedImageUrl=https://example.com/image.jpg
Accept: application/json

> {%
client.test("퀴즈 답변 제출(이미지) - 200", () => {
  client.assert(response.status === 200, "Status must be 200 OK");
});
%} 

### 내 퀴즈 답변 제출 (텍스트)
POST {{host}}/api/user-activities/missions/1/submit-quiz/me?textAnswer=정답
Accept: application/json

> {%
client.test("내 퀴즈 답변 제출(텍스트) - 200", () => {
  client.assert(response.status === 200, "Status must be 200 OK");
});
%}

### 내 퀴즈 답변 제출 (선택지)
POST {{host}}/api/user-activities/missions/1/submit-quiz/me?selectedChoiceIndex=0
Accept: application/json

> {%
client.test("내 퀴즈 답변 제출(선택지) - 200", () => {
  client.assert(response.status === 200, "Status must be 200 OK");
});
%}

### 내 퀴즈 답변 제출 (이미지)
POST {{host}}/api/user-activities/missions/1/submit-quiz/me?uploadedImageUrl=https://example.com/image.jpg
Accept: application/json

> {%
client.test("내 퀴즈 답변 제출(이미지) - 200", () => {
  client.assert(response.status === 200, "Status must be 200 OK");
});
%} 
